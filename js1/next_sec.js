// Write - Your - Code
const hours = Number(prompt("Enter hours:"));
const mins = Number(prompt("Enter minutes:"));
const seconds = Number(prompt("Enter seconds:"));
const next_sec = seconds+1;

alert(`In a second, the time will be ${hours}:${mins}:${next_sec}`);
